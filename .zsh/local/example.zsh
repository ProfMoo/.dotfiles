#!/bin/zsh
# Example local-only zsh file. Copy to e.g. aliases.zsh and edit.
# Anything in .zsh/local/ except README.md and this example is gitignored.

# Example: work-specific AI agent aliases
# alias claude='cbcode --agent claude'
# alias opencode='cbcode --agent opencode'
